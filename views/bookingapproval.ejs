<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bike Rental Systemt</title>
    <link rel="stylesheet" type="text/css" href="/styles/users.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body>
    <header>
        <h3>Bike rental system</h3>
        <nav>
            <ul>
              <form action="/adminlandingpage"  method="post">
                <button type="submit" name="button" value="home">AdminHome</button>
                </form>
          
            </ul>
        </nav>
     </header>
     <div class="My-Div">
         <div class="order-div">
              <table  class="styled-table" style="width:70%">
                <tr>
                    <th>Order_id</th>
                    <th>Bike_Number</th>
                    <th>user_name</th>
                    <th>Rental_price</th>
                    <th>payment_status</th>
                    <th>Booking Date</th>
                    <th>License image</th>
                    <th>Status</th>
                </tr>
                <% for(let item of items){ %>
                <tr>
                    <td><%= item.orderid %></td>
                    <td><%= item.bike_number %></td>
                    <td><%= item.user_name %></td>
                    <td><%= item.rental_price%></td>
                    <td><%= item.payment_status %></td>
                    <td><%= item.bookingdate%></td>
                    <td><img src= "data:image/jpeg;base64,<%=item.license_image%>" class="card-img-top" alt="new"/></td>
                    <td><form action="/orderstatus" method="post">
                      <input type="text" name="bike_number" value="<%= item.bike_number %>" readonly style="color: transparent; background-color: transparent; border: none">
                        <select id="dropdown" name="verification">
                            <option value="Pending">Pending</option>
                            <option value="Approved">Approved</option>
                            <option value="Rejected">Rejected</option>
                        </select>
                        
                        <input type="text" name="useremail" value="<%= item.useremail %>" readonly style="color: transparent; background-color: transparent; border: none">
                        <button type="submit" class="userUpdate"   name="orderid" value="<%=item.orderid%>">Submit</button>
                    </form></td>
                </tr>
                <% } %>
              </table>

              </body>
              </html>
              
           </div>
    </div>
</div>
        <footer  >
          <div class="col-md-4 d-flex align-items-center">
            <a href="/" class="mb-3 me-2 mb-md-0 text-body-secondary text-decoration-none lh-1">
              <svg class="bi" width="30" height="24"><use xlink:href="#bootstrap"></use></svg>
            </a>
            <span class="mb-3 mb-md-0 text-body-secondary" style="font-size: medium; font-weight: bold;"  >© 2023 Company, Inc</span>
          </div>
        </footer>
  
     


 
</body>
</html>